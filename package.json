{
  "name": "niivue-apps",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@electron-forge/maker-deb": "^6.3.0",
    "electron-installer-debian": "^3.1.0",
    "lerna": "^6.6.2"
  },
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "dev": "npx lerna run dev --stream --parallel",
    "start": "npx lerna run start --scope nv --stream",
    "build": "npx lerna run build",
    "graph": "npx nx graph"
  }
}
