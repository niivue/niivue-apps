{
  "name": "niivue-apps",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "dev": "npx lerna run dev --stream --parallel",
    "start": "npx lerna run start --scope desktop --stream",
    "build": "npx lerna run build",
    "graph": "npx nx graph"
  }
}
